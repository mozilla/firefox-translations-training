name: opuscleaner
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    # update to a normal pypi version when released
    - opuscleaner==0.4.0
    - fasttext==0.9.2
    - sacremoses==0.0.53
    - more_itertools==10.1.0
    - requests==2.31.0
