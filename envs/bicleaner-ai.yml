name: bicleaner-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - pip==21.2.2
  - cmake=3.21.1
  # disable if cudnn is already installed on a machine
  - cudnn=8.2.1.32
  - pip:
    - tensorflow==2.3.2
    - bicleaner-ai==1.0.1